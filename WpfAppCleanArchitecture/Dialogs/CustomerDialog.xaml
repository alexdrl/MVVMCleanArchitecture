<Window x:Class="WpfAppCleanArchitecture.Dialogs.CustomerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfAppCleanArchitecture.ViewModels"
        xmlns:res="clr-namespace:WpfAppCleanArchitecture.Resources"
        mc:Ignorable="d"
        Title="{x:Static res:Strings.WindowTitleCustomer}" Height="210" Width="300"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance Type=vm:CustomerDialogViewModel, IsDesignTimeCreatable=False}">
    <StackPanel Margin="20">
        <TextBlock Text="{x:Static res:Strings.LabelCustomerName}" />
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0" />
        <TextBlock Text="{x:Static res:Strings.LabelLastName}" Margin="0,10,0,0" />
        <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="{x:Static res:Strings.ButtonSave}"
                    Width="75"
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Command="{Binding SaveCommand}"
                    IsEnabled="{Binding CanSave}" />
            <Button Content="{x:Static res:Strings.ButtonCancel}"
                    Width="75"
                    IsCancel="True"
                    Command="{Binding CancelCommand}" />
        </StackPanel>
    </StackPanel>
</Window>